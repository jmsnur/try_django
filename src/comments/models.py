from django.conf import settings
from django.db import models



# class Commet(models.Model):
	# user =  models.ForeignKey(settings.AUTH_USER_MODEL)
	# to_post = models.ForeignKey(settings.)
	# content = 
	# timestamp = 
	# reply = 


